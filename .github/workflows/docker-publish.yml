name: Publish Docker image

on:
  push:
    branches: [ "main" ]
    tags: [ 'v*.*.*' ]

permissions:
  contents: read
  packages: write
  security-events: write 

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  push_to_registries:
    name: Push Docker image to multiple registries
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4  # CORRECTION: v5 n'existe pas, utilisation de v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3 # CORRECTION: Utilisation d'un tag de version simple (v3) plutôt que le hash long
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            # REMPLACER CECI par votre vrai nom de repo Docker Hub
            my-docker-hub-namespace/my-docker-hub-repository
            ghcr.io/${{ github.repository }}
          # AJOUT: Génère le tag sha-xxxx et latest pour s'assurer que Trivy trouve l'image
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=sha

      - name: Build and push Docker images
        id: push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@0.28.0
        with:
          # CORRECTION: On scanne l'image qu'on vient de construire sur GHCR
          image-ref: 'ghcr.io/${{ github.repository }}:sha-${{ github.sha }}' 
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v1 # CORRECTION: v3 n'existe pas, v1 est la stable
        with:
          subject-name: ghcr.io/${{ github.repository }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
